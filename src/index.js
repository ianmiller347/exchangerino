import React from 'react';
import ReactDOM from 'react-dom';
import Exchangerino from './Exchangerino';

import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <Exchangerino />
  </React.StrictMode>,
  document.getElementById('root')
);
